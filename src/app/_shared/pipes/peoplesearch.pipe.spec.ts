import { PeopleSearchPipe } from './peoplesearch.pipe';

describe('People.SearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PeopleSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
