import { FulladdressPipe } from './fulladdress.pipe';

describe('FulladdressPipe', () => {
  it('create an instance', () => {
    const pipe = new FulladdressPipe();
    expect(pipe).toBeTruthy();
  });
});
